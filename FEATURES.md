# 功能列表

说明：

- **作用面**
  - **客户端**：改本机表现或本地数据；每人需自己开。
  - **服务端**：改主机/房间规则；通常仅房主开启即可影响整局（访客未装 Mod 也可体验部分效果）。
- **默认**：对应 `DT_Tools.cfg` 中该功能段的 `Enabled`。
- **配置段**：`.cfg` 中的 `[Section]` 名。

运行时可用 Web 控制台 **`DT CONFIG`** 改参数；`Enabled` 变更后需保存并重启游戏才装卸补丁。

---

## Mod 开发（客户端）

| 功能 | 说明 | 默认 | 配置段 |
|------|------|:----:|--------|
| 游戏测试模式 | `IsPlaytestApp` 设定为 true；支付/库存仍走正式服 URL | 🔴 禁用 | `IsPlaytestApp` |

## 游玩体验（客户端）

|           功能           |                     说明                      |   默认   |         配置段          |
| ------------------------ | -------------------------------------------- | :-----: | ---------------------- |
| 表情无冷却                | 连续使用表情，跳过冷却                          | 🟢 启用 | `UseEmotion`           |
| 黑方攻击距离              | 自定义攻击索敌最大距离（默认 224）              | 🔴 禁用 | `GetTargetPlayer`      |
| 黑幕递刀距离              | 自定义递交武器最大距离（默认 224）              | 🔴 禁用 | `GetHandWeaponTarget`  |
| 全状态移速                | 对局内按状态调整移速倍率                        | 🔴 禁用 | `FixedUpdateSurvive`   |
| 大厅移速                 | 调整大厅移动倍率                               | 🔴 禁用 | `FixedUpdate`          |
| 伪证解锁                 | 审判讨论阶段任意身份可做伪证（内容同黑方）        | 🔴 禁用 | `CanShowLie`           |
| 移除迷雾                 | 移除房间阴影效果                               | 🔴 禁用 | `OnGhostVisualChanged` |
| 玩家雷达                 | 平板电脑显示全部玩家位置（白方无法区分黑方/黑幕） | 🔴 禁用 | `RefreshPlayerPin`     |
| 亡者呢喃                 | 那些死者的回响依附在你的身边                    | 🔴 禁用 | `EnqueueNormalChat`    |
| 福尔摩斯                 | 搜索线索读条时长（可与 ScanUp 规则叠加）         | 🔴 禁用 | `StartScanning`        |
| 专业清洁                 | Dark/Black 销毁证据本地读条时长                | 🔴 禁用 | `UseSabotage`          |
| 专业电工                 | 修电闸读条时长（优先结算来自服务端的剩余时间）    | 🔴 禁用 | `FuseboxInteract`      |
| 吾之真名                 | 在任何阶段都显示自己的名字（包括幽灵）           | 🔴 禁用 | `ChangeMyPlayer`       |
| 我会永远盯着你            | 时刻视奸所有人                                 | 🔴 禁用 | `CharacterMapPin`      |
| 哼哼哼，坐山观虎斗（乱武） | 获得武器位置和可破坏电闸位置（黑幕地图）          | 🔴 禁用 | `DarkRadar`            |

## 商店（客户端）

| 功能 | 说明 | 默认 | 配置段 |
|------|------|:----:|--------|
| 表情包全解锁 | 本地视为拥有全部表情 | 🔴 禁用 | `OwnedEmoticonIds` |
| 角色全解锁 | 本地视为拥有全部可选角色（**不含梅德琳**） | 🔴 禁用 | `OwnedCharacterIds` |
| 梅德琳解锁 | 解锁 Madeline（101）并补齐缺失资源/入口 | 🔴 禁用 | `IncludeMadeline` |

## 游戏系统（服务端）

|     功能     |                          说明                          |     默认     |        配置段        |
| ----------- | ------------------------------------------------------ | :----------: | ------------------- |
| 房间人数上限 | 开房/进房人数上限与 Steam Lobby 容量                     |   🔴 禁用    | `CreateLobby`       |
| 房间人数下限 | 正式服最少开局人数（测试模式下仍为 0）                     |   🔴 禁用    | `LOBBY_MIN_PLAYER`  |
| 调查阶段修复 | 修复无凶手时 `StartDetective` 空引用；控制台进调查依赖此项 | **🟢 启用** | `StartDetective`    |
| 重复角色     | 多人可选同一角色（房主开启即可）                           |   🔴 禁用    | `PickCharacter`     |
| 许愿鱼池     | 你可能在鱼塘里钓出各种东西（甚至是武器）                   |   🔴 禁用    | `FishingRandomItem` |

## 整活（客户端）

| 功能 | 说明 | 默认 | 配置段 |
|------|------|:----:|--------|
|   幽灵视角   | （⚠️奇怪的功能）化身幽灵👻；`IsGhostView` 设置为 true；易干扰正常对局，仅建议调试 | 🔴 禁用 | `IsGhostView` |
| 每日登录奖励 |                  登录游戏后自动领取实验数据（无需参与游戏）                       | 🔴 禁用 | `LoginReward` |

## 控制台

配置段：`[WebConsole]`（默认 **🟢 启用**，端口 **19450**，可选密码）。

浏览器：<u>[http://127.0.0.1:19450](http://127.0.0.1:19450)</u>

具体命令名、用法以控制台内 `/help` 为准。